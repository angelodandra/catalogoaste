import { Suspense } from "react";
import PrintClient from "./PrintClient";

export default function Page(props: any) {
  return (
    <Suspense fallback={<div className="mx-auto max-w-4xl p-6">Carico stampa…</div>}>
      <PrintClient {...props} />
    </Suspense>
  );
}
